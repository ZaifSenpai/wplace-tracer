name: Build Stage

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run dev-all

      - name: Renaming dist directory
        run: |
          REPO_NAME=$(echo ${{ github.repository }} | awk -F / '{print $2}')
          BRANCH_NAME=$(echo ${{ github.ref }} | awk -F / '{print $3}')
          NEW_NAME_CHROME="$REPO_NAME-chrome-dev"
          NEW_NAME_FIREFOX="$REPO_NAME-firefox-dev"
          echo "NEW_NAME_CHROME=$NEW_NAME_CHROME" >> $GITHUB_ENV
          echo "NEW_NAME_FIREFOX=$NEW_NAME_FIREFOX" >> $GITHUB_ENV

      - name: Upload chrome build as artifact
        uses: actions/upload-artifact@v4.6.1
        with:
          name: ${{ env.NEW_NAME_CHROME }}
          path: dist/chrome/*
          retention-days: 5

      - name: Upload firefox build as artifact
        uses: actions/upload-artifact@v4.6.1
        with:
          name: ${{ env.NEW_NAME_FIREFOX }}
          path: dist/firefox/*
          retention-days: 5
